/* On window load */
window.onload = function() {
    Particles.init({
        selector: '#particles-background',
        color: 'white',
        maxParticles: 150,
        speed: 0.25,
        sizeVariations: 5,
    });

    var scrollMagicController = new ScrollMagic();
    // TODO

};
